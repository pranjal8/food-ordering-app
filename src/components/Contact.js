import React from "react";
import { container } from "./Cart";

const Contact = () => {
  return (
    <div style={container}>
      <h1>Contact</h1>
      <form action="">
        <input type="text" />
        <input type="text" />
        <button>Submit</button>
      </form>
    </div>
  );
};

export default Contact;
